<header class="nikunabi_header">
  <div class="nikunabi_sub_nav container">
    <nav class="navbar nikunabi-navbar">
      <div class="nikunabi-navbar-header navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarEexample7">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <h1 class="h5 nikunabi_title">
        <span><%= link_to image_tag('nikunabi_logo.png', { :border => '0', :alt => 'logp'}), root_path, id: "logo", class: "nikunabi-logo" %> </span>
        なごや焼肉情報サイト ~ ニクナビ ~
        </h1>
      </div>
      
      <div class="collapse navbar-collapse" id="navbarEexample7">
        <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'ニクナビタイムズ', :blogs %></li>
        <li></li>
        <hr class="sp-only">
          <% if shop_signed_in? %>
            <li><%= link_to "マイページ", shops_root_url(current_shop) %></li>
            <li><%= link_to "ログアウト", destroy_shop_session_path, method: :delete %></li>  
          <% else %>
            <li><%= link_to '店舗ログイン', :shop_session %></li>
          <% end %>
        </ul>
      </div>
  </nav>
  </div>

  <nav class="nikunabi-main-navbar navbar navbar-default <% if current_page?(root_path) %>mb-0<% end %>">
			<div class="container">
				<div class="navbar-header">
           <%= link_to image_tag('nikunabi_logo.png', { :border => '0', :alt => 'logp'}), root_path, id: "logo", class: "nikunabi-logo" %>
				</div>
		
				<div class="navbar-collapse">
        <dl class="searchbox navbar-right">
            <%= search_form_for(@search, url: search_shops_path, method: :get) do |f| %>
              <dt><%= f.collection_select :areas_id_in, Area.all, :id, :name, { :include_blank => "エリア選択" } %></dt>
              <dt><%= f.collection_select :recommends_id_in, Recommend.all, :id, :name ,{ :include_blank	=> "お好きな部位を選択" } %></dt>
              <dd><%= f.submit ("検索") , class: 'search-btn btn-pink'%></dd>
            <% end %>
          </dl>
				</div>
			</div>
		</nav>

<%
=begin
%>
<nav>
      <ul class="nav navbar-nav navbar-right">
      <% unless !shop_signed_in? %>
        <li><%= link_to "マイページ", shops_root_url(current_shop) %></li>
        <li><%= link_to "ログアウト", destroy_shop_session_path, method: :delete %></li>  
      <% end %>
      </ul>
    </nav>
<%
=end
%>


</header>
