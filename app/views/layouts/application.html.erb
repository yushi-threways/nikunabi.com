<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
    <%= render 'layouts/shim' %>
</head>
<body>
  <%= render 'layouts/header' %>
  <%= render 'layouts/flash' %>

     <div class="">
       <div class="container-fluid">
         <div class="row">
           <div class="container">
             <p><%= breadcrumbs separator: " #{content_tag(:i, '', :class=>'glyphicon glyphicon-chevron-right')} "%></p>
             <% unless current_page?(root_path) %>
             <hr>
             <% end %>
             <%= yield %>
           </div>
       	</div>
       </div>
       <%= render 'layouts/footer' %>
     </div>
   </body>
</html>
