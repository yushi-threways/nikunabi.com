<li class="new_top_list-item">
<div class="nikunabi_new-open">
          <div class="nikunabi_new-open-name">
            <div class="clearfix">
                <p class="col-xs-10">
                  <span class="new-open-name"><%= shop.name  %></span>　<span class="new-open-strc">
                  <% shop.areas.each do |sa| %>
                  <%= link_to "#{ sa.name }エリア", area_url(:areacode => sa.areacode) %></span>
                </p>   
                <p class="new-open-link col-xs-2">
                  <%= link_to "詳細を見る", area_shop_url(:area_areacode => sa.areacode, :id => shop) %>
                  <% end %>
                </p>
            </div>
          </div>
          <div class="clearfix">
            <div class="nikunabi_new-open-left nikunabi_new-ope-left-img">
              <div class="nikunabi_new-open-img">
              <% unless shop.details.nil? %>
                <%= image_tag shop.details.image.url, :size => "150x150" %>
              <% else %>
                <%= image_tag 'nikunabi_def.jpg', :size => "150x150" %>
              <% end %>
              </div>
            </div>
            <div class="nikunabi_new-open-left nikunabi_new-ope-left-detail">
              <div class="nikunabi_new-open-info">
                <% unless shop.details.nil? %>
                <p>営業時間：<%= shop.details.open %></p>
                <p>定休日：<%= shop.details.holiday %></p>
                <p>アクセス：<%= shop.details.access %><br></p>
                <p>喫煙・禁煙：<%= shop.details.smoking %></p>
                <p>お支払い方法：<%= shop.details.payment %></p>
                <% end %>
              </div>
            </div>
          </div>
          <div class="nikunabi_shop_tag">
            <ul class="nikunabi_shop_tag-recommend">
              <% shop.recommends.each do |recommend| %>
                <li><%= recommend.name %></li>
              <%- end %>
            </ul>
            <ul class="nikunabi_shop_tag-feature">
              <% shop.features.each do |feature| %>
                <li><%= feature.name %></li>
              <%- end %>
            </ul>
          </div>
        </div>
      </li>