<% provide(:title, "店舗メニュー情報") %>

<% content_for :page_h1 do %>
<h1>
  店舗メニュー情報
  <small>店舗メニュー情報</small>
</h1>
<ol class="breadcrumb">
  <li><a href="#"><i class="fa fa-dashboard"></i> Level</a></li>
  <li class="active">Here</li>
</ol>
<% end %>

  <div class="row">
    <div class="col-md-12">
      <div class="box box-info">
        <div class="box-header with-border">
          <h3 class="box-title">店舗メニュー情報</h3>
        </div><!-- /.box-header -->
        <div class="box-body no-padding">
          <p id="notice"><%= notice %></p>

          <table class="table table-striped">
            <thead>
              <tr>
                <th>メニュー名</th>
                <th>メニュー画像</th>
                <th>商品価格</th>
                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @menus.each do |menu| %>
                <tr>
                  <td><%= menu.name %></td>
              <td class="mypage-shop-menu-img"><%= image_tag menu.image.url %></td>
                  <td><%= menu.price %></td>
                  <td><%= link_to '編集', edit_menu_path(menu), class: "btn btn-primary" %></td>
                  <td><%= link_to '削除', menu, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
              </div>
          </div>
      </div>
    </div>
  </div>
