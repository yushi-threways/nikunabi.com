<%= form_with(model: shop_event, local: true) do |form| %>
  <% if shop_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shop_event.errors.count, "error") %> prohibited this shop_event from being saved:</h2>

      <ul>
      <% shop_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :images %>
    <%= form.text_field :images %>
  </div>

  <div class="field">
    <%= form.label :published_at %>
    <%= form.date_select :published_at %>
  </div>

  <div class="field">
    <%= form.label :closed_at %>
    <%= form.date_select :closed_at %>
  </div>

  <div class="field">
    <%= form.label :published %>
    <%= form.check_box :published %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
